# 更新日志 (Changelog)

本项目遵循 [Semantic Versioning (语义化版本 2.0.0)](https://semver.org/lang/zh-CN/) 规范。

## [v1.3.0] - 2026-09-18

### 🎨 UI 通用能力抽离与 iPhone 灵动岛多风格一键切换 (AIR-37)
- **独立顶层 UI 规范先行 (`specs/ui-design-system.md`)**：
  - 建立统一 Design Tokens 调色板、字号阶梯、弹性动效曲线与人机大白话词表，作为全局 UI/UX 演进的事实源；
- **双主题架构与严格 CSS 作用域隔离 (Zero-Pollution Guarantee)**：
  - 保留原有经典的工整极客控制台 (`classic`) 作为出厂默认基线，100% 绝对保护现有使用习惯；
  - 抽离 `ThemeManager`（`classic` / `island`），基于 `html[data-theme="..."]` 与 `localStorage`（`cellular_gateway_theme`）实现秒级无缝热切换；
  - 顶栏右侧与系统设置抽屉接入“🎨 视觉风格”切换入口；
- **iPhone 灵动通信岛组件 (`#dynamicIsland` & `DynamicIslandController`)**：
  - 抽离前端轻量 `EventBus`，彻底解耦物理通信事件分发与界面渲染；
  - 待机态呈现居中悬浮黑洞胶囊，动态响应底层实际 `slots` 卡槽数量（单卡单指示灯、多卡多指示灯，单多卡工程通用）；
  - 收到新验证码时以水滴果冻流体弹性展开为 76px 空间悬浮卡片，展示金黄钥匙图标、卡槽来源、大字号等宽高亮验证码数字与复制胶囊；
  - 内置悬停保护看门狗（Hover Pause Watchdog）：鼠标悬停时自动冻结 10s 回缩定时器，防止误触；
- **消息排版大白话提炼与全域点击秒级直达 (`FeedFormatter`)**：
  - 自动提炼正文首部机构签名（如 `【深度求索】`、`【中国联通】`），提升为主标题，消除 16 位长号杂音；
  - 短信正文内的验证码数字自动渲染为 `.otp-text-highlight` 晶体底衬，点击正文数字同样秒级直写 Windows 剪贴板；
  - 封装全局通用剪贴板工具 `copyTextWithFallback()`，双层兜底确保复制必达；
- **Apple 空间微拟物折射、iOS 翠绿开关与 6px 极窄滚动条**：
  - 灵动岛模式下硬件卡片注入 `0 1px 0 rgba(255,255,255,0.14) inset` 晶体微拟物折射高光；
  - 4G 蜂窝数据开关切换为 iOS 经典翠绿色（`#10b981`）；
  - 全局覆写定制 6px 极窄暗黑半透明滚动条，消灭 Windows 原生粗白滑块；
- **全端自适应与双工程严格镜像**：
  - 灵动岛宽度采用 `min(440px, calc(100vw - 24px))` 配合移动端顶栏动态补白，360×812 移动端 0 溢出穿模；
  - `tools/cluster_gateway` 与 `tools/host_gateway` 的 `web/index.html` 保持 100% 字节级严格镜像同步。

## [v1.2.9] - 2026-09-18

### 🚀 工业级重大里程碑更新 (AIR-28 ~ AIR-36 全量闭环)
- **上位机 MCP 零信任授权门禁与多卡集群调用治理 (AIR-36)**：
  - **方案 D 架构**：Web 控制台系统设置抽屉新增 AI 智能体通信服务 (MCP) 硬开关，出厂严格默认关闭 (Default OFF)；
  - **内部免检信任环**：Web 控制台下发操作携带 Session Token 免检放行，彻底杜绝误杀；外部 MCP 未授权时 100% 物理拦截敏感操作（发信/拨号/读短信/改网卡/重启等 403 拒绝）并阻断实时短信广播，只读探针放行并注入大白话自解释引导；
  - **客户端孤儿进程根除与 Fast-fail**：彻底移除 `_auto_spawn_hub()` 根除后台僵尸进程；MCP 关闭时 `wait_for_otp` 毫秒级快速报错拒绝，彻底消除了未授权场景下盲等 20 秒超时的缺陷；
  - **1~N 多板卡集群治理**：透传型号/芯片架构/号码/运营商/VoLTE/FOTA 硬件能力元数据全景，支持同网亲和出站分流策略与 VoLTE 优选兜底；
  - **Apple 风毛玻璃与 AI 提示词模块**：设置抽屉增加 `12px` 高斯高保真磨砂毛玻璃遮罩，支持点击背景或按 `ESC` 自然收起；抽屉内内置标准 AI 提示词与一键复制按钮。
- **多芯片通用固件线刷与全新模组接入向导 (AIR-35)**：
  - **脱离 Luatools 依赖**：固化移芯原厂 `FlashToolCLI.exe` 与 EC718PV、EC618 官方验证底座（`tools/flasher/`），支持 Full Flash 全量灌入（10秒）与 Script Flash 极速更新（2秒）；
  - **全新模组接入向导**：自动安全嗅探出厂 AT 态与 Bootloader 态未分配端口，开箱板卡插上一键全量固化为通信网关。
- **状态保活防抖与硬件刷机加固 (AIR-34)**：
  - 信号柱物理前置，左侧栅格加宽消除两点截断；建立持久属性粘性缓存（Sticky Cache）与安全增量 DOM 更新，阻断定时器倒退为“读取中...”；
  - 注入 `_SafeStream` 根除 Windows 无控制台进程 `[Errno 22]` 崩溃，伴生控制端口安全下发软复位切入 Bootloader。
- **短信清除标记加固与真实硬件数据收敛 (AIR-33)**：
  - 加固时间归一化秒级整型指纹与三维复合墓碑（ID + 指纹 + 内容模糊），修复响应帧解包兼容，彻底根除清除后刷新复活缺陷；
  - 生产卡仓严格收敛为模组底层真实持有的联通短信。
- **去伪存真工业级双保底架构 (AIR-32)**：
  - 彻底清除前端假刷机动画、假删除、离线假成功及写死假数据；打通物理线刷独占避让通道与真实 SSE 流式终端输出；
  - 前端上线“🧹 清屏聚焦模式”（视图过滤防打扰且支持一键无损还原）。
- **多模组展示去工程化与四级运营商推导加固 (AIR-31)**：
  - 建立 ICCID + 本机号段 + 特服号发件人 + 首部签名严格四级运营商推导体系，彻底消灭“未知运营商”；
  - 重构收件箱紧凑融合大白话标签（如 `Air780EPV · 联通 2515`）。
- **单卡/多卡 VoLTE 电话呼叫与硬件能力门禁 (AIR-30)**：
  - 支持板端 15s 自动挂机看门狗与接听秒挂断（0话费防线）；上位机增加硬件差异 Fast-fail 门禁。
- **多模组串口在线平滑升级 (AIR-29 Serial SOTA)**：
  - 纯 Python 实现 LuaDB 与 SOTA 打包流水线，2 秒极速热更业务脚本，48 条短信黑匣子无损留存。
- **自适应网关 UI 与大白话交互重构 (AIR-28)**：
  - 左侧手风琴就地展开、四大运营商原版矢量 SVG、右侧双 Tab 平级聚合、验证码右上角胶囊统一定位与大白话去工程化清洗。

---

## [v1.2.5] - 2026-09-13

### 新增与优化
- **Push ACK 双向握手与宽带代推优先闭环 (AIR-17)**：
  - 彻底纠偏“板端蜂窝开启上位机就退让”的旧逻辑：只要上位机在网运行，强制优先通过宿主宽带代发飞书/Bark，锁定手机卡蜂窝数据消耗为 0 字节；
  - 宽带代推成功后，上位机毫秒级向板端串口回写 `notify_ack [ok]`，板端收到后注销 5.0 秒自推定时器；若全渠道失败或上位机断线，板端 5 秒后触发 4G HTTP 应急自推；
- **防串台路由隔离**：集群版 `gateway_hub.py` 实施严格卡槽定向隔离，指定 slot 离线时严禁兜底降级回退到 slot_1，避免跨卡槽短信串台；
- **单文件应用升级**：重新打包构建 `Air780EPV-Gateway.exe`（27.27 MB），注入 `_SafeStream` 防 NoneType 崩溃，全链路自动化回归验收 11/11 项通过。

---

## [v1.2.4] - 2026-09-12

### 新增与重构
- **单卡稳定版与集群版物理彻底分治 (AIR-19)**：
  - 彻底拆分代码目录为 `tools/host_gateway/`（单卡稳定纯净版，去除卡槽横条干扰）与 `tools/cluster_gateway/`（多设备集群版）；
  - 单卡版 Web 前端恢复直观顶部 OTP 胶囊与极简收件箱，集群版提供弹性会话池与卡槽切换；
- **串口自适应动态探测与热拔插漂移自愈**：
  - 弃用硬编码 COM 口，基于 `19D1:0001` + `x.6` (VUART_0) 拓扑特征自动定位通信端口，毫秒级自愈 Windows 端口重分配漂移；
- **来电暗号拦截与 FOTA 实机闭环 (AIR-18)**：
  - 逆向分析 LuatOS 底层固件，补齐 92 字节 SOTA 头注入与 LZMA 压缩流水线；
  - 建立 0 话费纯信令来电秒挂机制，支持拨打指定管理员白名单电话触发免插线固件空中更新。

---

## [v1.2.0] - 2026-09-11

### 新增与修复
- **防 OOM 游标分页与触底无感懒加载**：
  - 针对板端 128KB 极限内存，确立 `h:gen:offset` 游标分页，单批锁定 15 条且截断至 256 字节，峰值 RAM 恒定在 12KB 以内；
  - 前端基于 `IntersectionObserver` 160px 阈值哨兵感知与三级判空，彻底消除连续刷新导致的板端 OOM 重启缺陷；
- **双重默认关闭与纯信令 0 流量保号 (AIR-08)**：
  - 出厂默认物理掐断 USB RNDIS 网卡与板载蜂窝数据通信，实现 0 流量保号防线；
- **Windows 免安装单文件发布 (AIR-14)**：
  - 基于 PyInstaller 打包生成独立绿色桌面程序，集成 Win32 Mutex 单实例互斥、系统托盘常驻与 Edge 原生独立 App 窗口自动唤起。

---

## [v1.0.0] - 2026-09-10

### 初始发布
- **全网通短信与 0 话费秒挂中枢 (P0~P2)**：
  - 基于移芯 EC718P-V 硬件原生 VoLTE / IMS 协议栈，全面支持移动、联通、广电及电信（SMS over IMS）短信；
  - 内置工业级 3 层正则引擎，秒级提码并过滤干扰数字；
- **FastMCP 物理工具服务 (AIR-06)**：
  - 封装标准 stdio MCP 协议，暴露 7 大物理通信工具供 AI Agent 调度；
- **《硅基涌现》Plugin Platform 4.0 官方插件 (AIR-09)**：
  - 提供硬件状态看板、OTP 动态挂载与双向桥接能力。
